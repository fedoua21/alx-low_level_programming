#include "main.h"
/**
 * _abs - compute the absolute value of integr
 * @i: input number as an integr
 * return: absolute value
*/
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
